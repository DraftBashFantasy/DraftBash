import { IDraftOrderGenerator } from '../IDraftOrderGenerator';
export declare class SnakeDraftOrderGenerator implements IDraftOrderGenerator {
    generate(numberOfTeams: number, teamSize: number): number[];
}
